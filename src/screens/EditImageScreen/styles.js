import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    alignSelf: 'center',
    width: 300,
    height: 300,
    resizeMode: 'contain',
  },
});

export default styles;
